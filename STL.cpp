#include <iostream>
#include <vector>
#include <queue>
#include <stack>
#include <map>
#include <algorithm>


using namespace std;
// vector<int> a; a.push_back(10); a.size();a.clear();a.begin(), a.end();
// vector<int> b[100]; 100个vector数组
// stack<type> s; s.push(item); s.top();s.pop(); s.size(); s.empty();
// queue<type> q; q.push(item); q.pop(); q.back(); q.size(); q.empty()

//struct cmp //重写仿函数
//{
//    bool operator()(int a, int b)
//    {
//        return a < b; //大顶堆
//    }
//};
//priority_queue<int, vector<int>, cmp> aa;
// set<type> a;
// a.insert(); a.erase();a.find();a.clear();a.empty();a.size()
// map<string, int> m; m["tom"] = 100; m.clear(); m.size(); m.count();
// nth_element(begin, k, end)
// next_permutation(begin, end, cmp)

int main()
{

    return 0;
}




































